# DECISIONS

## 2026-06-16 用最终验收矩阵约束收口边界

### 决策内容

新增 `FINAL_ACCEPTANCE.md`，把当前版本的功能验收、内容验收、技术验收、部署验收、明确未做范围、剩余收口估算和最终通过标准集中记录。

### 决策原因

项目已经具备完整静态学习平台能力，继续无边界追加功能会导致“永远差一点”。最终阶段需要把交付标准显式化，让后续工作聚焦在验证、修复和部署确认，而不是继续扩张。

### 被否决方案

- 继续只在聊天中说明剩余时间：不可追溯，新的 AI 接管后容易丢失。
- 把验收标准散落在 README 和 DEPLOY：不利于快速判断最终交付边界。
- 立即引入账号、云同步或外部 AI 调用：会显著增加部署、隐私和维护成本，不适合作为当前静态版本收口项。

### 后续影响

- 后续判断项目是否结束时，优先按 `FINAL_ACCEPTANCE.md` 的最终通过标准执行。
- 新增大功能前必须重新评估剩余时间和交付边界。

## 2026-06-16 README 作为部署健康入口

### 决策内容

在 `README.md` 顶部增加 GitHub Actions Verify 徽章和“项目状态”区，直接展示公网访问地址、远端验收入口、GitHub + Vercel 部署口径和 `npm run verify`。

### 决策原因

项目已经具备本地总验收、GitHub Actions 和 Vercel 生产部署。继续把这些信息分散在 README 中后段、DEPLOY 或状态文件里，会增加用户和新 AI 判断当前项目是否健康的成本。README 顶部应作为最短确认路径。

### 被否决方案

- 只保留 DEPLOY 文档说明：适合详细部署，不适合快速判断健康状态。
- 新增单独状态页面：会增加维护入口，当前 GitHub README 足够。
- 只写公网 URL 不放 CI 徽章：无法快速看到远端验收状态。

### 后续影响

- 后续若公网域名或 Actions 工作流文件名变化，需要同步更新 README 顶部状态区。
- 接管项目时优先从 README 顶部判断访问地址和验收入口。

## 2026-06-16 GitHub Actions 使用 npm run verify 作为远端验收

### 决策内容

新增 GitHub Actions 工作流 `.github/workflows/verify.yml`，在 push 到 `main` 和 pull request 指向 `main` 时使用 Node.js 20 执行 `npm ci` 与 `npm run verify`。

### 决策原因

项目已经有本地总验收命令，但只依赖本机执行仍可能出现漏跑。把同一条命令接入 GitHub Actions，可以在代码进入远端仓库时自动复查内容、视频、类型、Lint 和生产构建，保证 GitHub + Vercel 部署链路更稳定。

### 被否决方案

- 只依赖 Vercel 构建：Vercel 只执行生产构建，不能覆盖内容审计和视频审计。
- 在 Actions 中拆成多条不同命令：会与本地验收口径分叉，维护成本更高。
- 跳过视频审计：会降低稳定性，但不符合当前视频链接必须可访问的要求。

### 后续影响

- GitHub Actions 失败时，应先修复失败项，再继续部署。
- 后续新增验证项时，优先加入 `npm run verify`，Actions 会自动继承。
- `audit:videos` 可能受远端网络波动影响，需要结合日志判断是真失效还是临时网络问题。

## 2026-06-16 重新开启合盖继续运行 Codex 的系统级电源策略

### 决策内容

按用户最新要求，重新设置 MacBook 合盖后不影响 Codex 继续运行任务：

```bash
pmset -a sleep 0 displaysleep 0 disksleep 0 disablesleep 1
```

执行后当前配置为：

- Battery Power：`sleep 0`、`displaysleep 0`、`disksleep 0`
- AC Power：`sleep 0`、`displaysleep 0`、`disksleep 0`
- `SleepDisabled`：`true`

### 决策原因

用户再次明确要求设置 MacBook 关闭盖子也不影响 Codex 继续跑任务。Codex 进程自身持有空闲睡眠断言，但合盖场景需要系统级电源策略配合，因此重新开启 `disablesleep` 并关闭系统、显示器和磁盘睡眠计时。

### 被否决方案

- 只依赖 Codex 的 `NoIdleSleepAssertion`：不能充分覆盖合盖睡眠场景。
- 只运行临时 `caffeinate`：更适合短任务，无法持久表达当前用户要求。
- 忽略此前为散热安全取消的记录：会破坏状态可追溯性；本次保留历史并追加最新决策。

### 后续影响

- 合盖运行会增加发热和耗电风险，不应直接放入包内长时间运行。
- 如需再次取消并恢复正常睡眠，可按 2026-06-13 记录恢复为 `SleepDisabled=false` 和改动前电源计时。

## 2026-06-16 部署前验收统一为 npm run verify

### 决策内容

新增 `npm run verify` 作为交付前总验收命令，依次执行 `export:content`、`audit:content`、`typecheck`、`lint`、`audit:videos` 和 `build`。

### 决策原因

项目现在已有内容导出、内容审计、视频审计、类型检查、Lint 和构建验证。继续要求人工逐条记忆命令容易漏跑，尤其是内容审计和视频审计。统一入口能降低部署前人为遗漏，便于新 AI 或用户直接接管。

### 被否决方案

- 只在文档里列出多条命令：仍依赖人工记忆和手动执行顺序。
- 把视频审计排除在总验收外：会提高命令稳定性，但不符合“不能保留打不开视频”的交付标准。
- 引入额外任务运行器：当前 npm scripts 足够，不需要增加依赖。

### 后续影响

- 提交 GitHub 或导入 Vercel 前优先运行 `npm run verify`。
- 单项命令仍保留，便于定位具体失败环节。
- 如果未来新增检查项，应优先加入 `verify`。

## 2026-06-16 内容完整性审计进入固定 npm 脚本

### 决策内容

新增 `npm run audit:content`，用 `scripts/audit-content.mjs` 固化内容完整性检查。该命令只读检查 `data/*.json` 和 `content/*.md`，不负责生成或修改内容；Markdown 生成仍由 `npm run export:content` 执行。

### 决策原因

项目内容规模已经较大，包含 Prompt、训练营、项目、工作流、模板、文章、视频、工具、安全售前和复盘内容。继续依赖临时 Node 命令做审计，不利于后续维护和 Vercel 部署前复查。固定脚本可以让内容数量、关键字段和 Markdown 备份状态成为可重复验收项。

### 被否决方案

- 把审计逻辑继续保留为临时命令：不可维护，也不利于新 AI 接管。
- 让审计脚本自动导出 Markdown：会让检查命令产生副作用，难以区分“检查”和“修复”。
- 引入外部 schema 校验库：当前项目不需要新增依赖，原生 Node 脚本足够覆盖现阶段内容模型。

### 后续影响

- 部署前验证流程增加 `npm run audit:content`。
- 后续新增数据集合、字段或内容数量要求时，需要同步更新审计脚本。
- 视频实时可访问性仍由 `npm run audit:videos` 负责。

## 2026-06-15 内容可信源继续采用 JSON 主源与 Markdown 导出备份

### 决策内容

继续以 `data/*.json` 作为页面渲染和内容维护的结构化主源，`content/*.md` 只作为 `npm run export:content` 自动生成的可读备份和审阅材料，不手工维护与 JSON 分叉的第二套内容。

### 决策原因

用户要求所有内容存储在 `data/*.json` 和 `content/*.md` 中，避免内容丢失。当前项目已经由组件直接消费 JSON 数据，同时 Markdown 可读性更好，适合 GitHub diff、人工审阅和交接。保持 JSON 主源、Markdown 导出备份，可以兼顾页面稳定性和内容可迁移性。

### 被否决方案

- 手工同时维护 JSON 和 Markdown：容易产生内容漂移，后续无法判断哪份为准。
- 改为只维护 Markdown：会增加解析复杂度，并破坏当前类型化数据模型。
- 引入 CMS 或数据库：超出当前 GitHub + Vercel 静态部署范围，会增加账号、权限和运维成本。

### 后续影响

- 新增或修改内容时必须优先修改 `data/*.json`。
- 内容变更后必须运行 `npm run export:content` 同步 `content/*.md`。
- 内容完整性审计字段口径以 `types/content.ts` 为准。

## 2026-06-15 六小时交付版收束为学习闭环

### 决策内容

将后续开发目标收束为“6 小时以内完成、尽可能完美可闭环”的版本。短期不再新增大模块、账号系统、后端服务、数据库、外部 AI 调用或复杂云同步，优先完成学习、项目、复盘、证据、备份、部署的闭环体验。本轮在 `/progress` 新增“学习证据库”，只读聚合已有 localStorage 学习资产并生成可复制的证据报告。

### 决策原因

项目已经具备内容、Prompt、训练营、项目、视频、复盘、进度、备份和部署能力。继续扩张功能会提高验证和维护成本，反而影响 6 小时内交付闭环。学习证据库能把现有能力串成完整成果链路，让用户明确看到“我学了什么、做了什么、沉淀了什么、下一步补什么”。

### 被否决方案

- 继续开发大型新模块：会拉长测试和部署周期，不利于 6 小时内收口。
- 引入账号和云同步：需要权限、隐私、数据库和部署配置，超出当前静态站点阶段。
- 调用外部 AI 生成证据评价：会引入 API Key、费用、隐私和稳定性风险。
- 新增单独证据存储 key：当前证据可以从已有学习资产实时推导，避免派生状态一致性问题。

### 后续影响

- 六小时版本后续只做闭环 QA、明显缺陷修复、文档和部署确认。
- 学习证据报告会随训练、项目、复盘和视频状态实时变化。
- 如后续引入云同步，应优先同步原始学习资产，再继续本地推导证据报告。

## 2026-06-15 学习路径推荐采用本地只读聚合

### 决策内容

在 `/progress` 新增“学习路径推荐”面板，聚合已有 localStorage 状态生成下一步行动队列。读取范围包括 30 天训练进度、视频观看记录、复盘历史和项目交付检查，不新增新的本地存储 key，也不调用外部模型。

### 决策原因

当前站点已经具备训练、项目、视频、复盘和备份能力，但用户仍需要自己判断下一步优先做什么。学习路径推荐能把分散状态转化为行动建议，符合“Learn Codex By Building”的产品定位，同时保持静态部署、隐私和可维护性。

### 被否决方案

- 调用大模型做个性化规划：会引入 API Key、费用、隐私和部署复杂度。
- 新增单独推荐历史状态：当前推荐可以从已有状态实时推导，新增派生状态会带来备份和一致性问题。
- 单独新建学习路径页面：进度中心已经承载学习状态，直接放在 `/progress` 更符合用户工作流。

### 后续影响

- 推荐结果会随用户训练、视频、复盘和项目状态变化。
- 后续如新增云同步，应优先同步原始学习资产，再由推荐面板继续本地推导。
- 推荐规则可以继续根据真实学习卡点迭代。

## 2026-06-15 Prompt 质量评分采用本地启发式规则

### 决策内容

在 `/task-builder` 中将原任务完整度评分升级为 Prompt 质量评分器。评分维度包括目标清晰度、上下文密度、约束边界、完成标准、验证计划和输出格式，所有评分均在浏览器本地用确定性规则计算，不调用外部模型，也不保存评分历史。

### 决策原因

Codex 学习的核心能力之一是写出可执行任务。原先只按字段长度计算质量，无法指导用户补齐真实工程任务所需的信息。启发式评分可以即时反馈 Prompt 的薄弱维度，同时保持静态部署、隐私和响应速度。

### 被否决方案

- 调用大模型进行评分：会引入 API Key、费用、隐私和部署复杂度。
- 保存评分历史：当前阶段价值低于复杂度，还会增加备份范围和状态同步问题。
- 单纯保留百分比完整度：反馈过粗，无法训练用户理解 Goal、Context、Constraint、DoD 和 Verification 的差异。

### 后续影响

- 评分只作为训练反馈，不承诺 Codex 一定成功执行。
- 后续如果增加评分历史，应同步加入本地备份白名单和恢复预览。
- 评分规则后续可继续按真实用户 Prompt 失败案例迭代。

## 2026-06-14 训练复盘启动器复用复盘中心草稿模型

### 决策内容

在 `/progress` 新增“训练复盘启动器”，根据 `codex-mastery:completed-days` 生成每日训练复盘草稿，并写入已有 `codex-mastery:review-drafts.review-daily-codex-training`。不新增复盘数据模型、不新增路由、不引入后端。

### 决策原因

用户完成训练后需要把学习结果沉淀为复盘，而当前进度中心和复盘中心之间缺少直接衔接。复用复盘中心草稿模型可以把学习进度、复盘模板、历史保存和 Markdown 导出能力串起来，同时保持静态部署和本地隐私策略。

### 被否决方案

- 新增独立训练复盘页面：会和现有复盘中心重复，增加导航和维护成本。
- 新增单独训练复盘 localStorage key：会让复盘数据分散，备份和历史保存需要额外适配。
- 自动跳转并自动保存历史：用户可能还未补充卡点和明日行动，自动保存会制造低质量历史。

### 后续影响

- 同步动作会覆盖每日训练复盘模板当前草稿，这是明确的手动操作。
- 本地学习档案备份已经覆盖 `codex-mastery:review-drafts`，无需新增备份 key。
- 后续可在复盘中心增加“来自训练进度”的提示，但当前先保持最小闭环。

## 2026-06-14 学习成就采用本地进度推导

### 决策内容

在 `/progress` 新增“学习成就与连续训练”面板，但不新增数据库、账号或新的持久化 key。成就、最长连续 Day、下一里程碑和每周下一项全部从已有 `codex-mastery:completed-days` localStorage 状态推导。`ProgressCenterClient` 在写入进度后广播 `codex-mastery:progress-updated` 事件，供成就面板同页实时同步。

### 决策原因

学习进度中心已有 30 天完成状态，但缺少阶段反馈和持续推进动机。通过本地推导成就可以提升交互体验，同时保持当前静态部署、本地隐私和备份恢复策略。由于备份能力已经覆盖 `codex-mastery:completed-days`，成就无需单独备份。

### 被否决方案

- 新增独立成就 localStorage key：会产生派生状态一致性问题，完成状态变化后还要额外同步。
- 做自然日签到系统：需要记录日期历史，当前 30 天训练更适合按 Day 编号推进。
- 引入账号和云端成就：超出当前静态学习平台阶段，会增加隐私、权限和部署复杂度。

### 后续影响

- 成就状态会随 30 天完成状态实时变化，取消某个 Day 后相关成就也会回退。
- 连续训练口径是连续 Day 编号，不是自然日连续登录。
- 后续如新增云同步，应优先同步原始完成状态，而不是同步派生成就。

## 2026-06-14 备份导入采用预览确认与选择性恢复

### 决策内容

将 `/progress` 的本地学习档案导入流程从“选择备份文件后立即恢复”改为“读取备份 -> 预览可恢复数据类 -> 用户勾选恢复范围 -> 确认后写入 localStorage”。默认勾选备份中可恢复的所有白名单数据，同时提供全选、清空、取消和单项勾选。

### 决策原因

备份恢复会覆盖浏览器本地学习进度、收藏、复盘历史、项目执行记录等资产。直接在文件选择事件里写入 localStorage 风险偏高，用户无法确认备份内容和覆盖范围。预览确认流程能保留静态部署、本地处理和隐私保护的优势，同时显著降低误恢复风险。

### 被否决方案

- 继续选择文件即恢复：交互效率高，但误覆盖风险高。
- 导入后弹浏览器原生确认框：只能确认一次整体操作，无法选择恢复范围。
- 引入云端版本管理：需要账号、数据库和权限模型，超出当前 GitHub + Vercel 静态站点阶段。

### 后续影响

- 导入确认前不会覆盖任何本地学习状态。
- 恢复动作只写入用户勾选的白名单 key。
- 后续如增加新的本地状态 key，需要同步加入备份白名单、类型校验和预览文案。

## 2026-06-13 本地学习档案备份采用白名单 JSON

### 决策内容

在 `/progress` 学习进度中心新增“本地学习档案：备份与恢复”面板，支持导出和导入本站白名单 localStorage 数据。备份文件为 JSON，包含 30 天进度、收藏、视频观看、复盘草稿、复盘历史、项目检查项、项目执行记录和主题设置。

### 决策原因

当前站点保持 GitHub + Vercel 静态部署，用户学习状态保存在浏览器 localStorage。随着复盘历史、项目执行记录和项目组合进度能力增多，清理浏览器或换设备会造成学习资产丢失。白名单 JSON 备份可以在不引入账号、数据库和后端的前提下，让用户自行保存和恢复学习档案。

### 被否决方案

- 引入账号和云端同步：会增加权限、隐私和部署复杂度，超出当前静态学习平台阶段。
- 导出整个 localStorage：可能包含非本站数据或未来敏感内容，不符合最小权限原则。
- 导入任意 JSON 写入 localStorage：风险过高，必须限制在本站已知 key 和预期数据类型内。

### 后续影响

- 备份恢复只覆盖本站白名单数据，导入完成后需要刷新页面让各模块重新读取状态。
- 用户需要自行保管备份文件；备份文件可能包含个人复盘和项目记录，不应随意分享。

## 2026-06-13 再次取消合盖继续运行 Codex 的系统级电源策略

### 决策内容

按用户最新要求，恢复本机 macOS 正常睡眠策略，取消“MacBook 关闭盖子也不影响 Codex 继续跑任务”的系统级设置，避免影响电脑正常散热：

```bash
pmset -b sleep 1 displaysleep 20 disksleep 10 disablesleep 0
pmset -c sleep 0 displaysleep 0 disksleep 10 disablesleep 0
pmset -a disablesleep 0
```

恢复后当前配置为：

- Battery Power：`sleep 1`、`displaysleep 20`、`disksleep 10`
- AC Power：`sleep 0`、`displaysleep 0`、`disksleep 10`
- `SleepDisabled`：`false`

### 决策原因

用户明确担心 MacBook 放在包里持续运行导致发热损坏，并要求“恢复原样，取消这个任务计划，不要影响我的电脑正常散热”。因此当前优先级从“合盖继续跑任务”调整为“合盖可正常睡眠和散热安全”。

### 被否决方案

- 保留 `SleepDisabled=true`：会让合盖后继续运行的风险变高，不符合用户最新安全诉求。
- 只提醒用户不要放包、不改设置：不能消除系统级配置带来的风险。
- 执行 `pmset restoredefaults`：可能改动超出本轮设置的其他电源项，不如按已记录的原始配置精确恢复。

### 后续影响

- 合盖后 Codex 长任务可能暂停或中断，这是当前为了散热安全接受的结果。
- 放入包内前应优先确认机器已睡眠或关机，避免携带过程中持续运行。

## 2026-06-13 项目实战中心增加本地项目组合总览

### 决策内容

在 `/projects` 页面顶部新增项目组合进度总览，基于 `codex-mastery:project-checklist` 汇总 8 个项目的整体完成度、检查项进度、完成项目数、建议优先推进项目，并支持复制项目组合进度报告。

### 决策原因

项目实战中心已有单项目交付检查清单和项目详情页，但用户连续推进多个项目时缺少全局视角。项目组合总览可以帮助用户快速判断当前训练进度、下一步优先项目，并把进度报告复制给 Codex 做周复盘或推进计划。

### 被否决方案

- 引入数据库统计进度：当前站点保持静态部署，本地状态足以覆盖个人学习场景。
- 只在每个项目卡片显示进度：无法形成跨项目优先级判断。
- 单独新增项目仪表盘页面：当前阶段会增加导航复杂度，先在项目实战中心顶部聚合更直接。

### 后续影响

- 总览只反映当前浏览器 localStorage 中的项目检查状态。
- 后续如增加项目数量，总览会自动按项目数据和检查项状态计算。

## 2026-06-13 项目执行记录导入采用浏览器本地 Markdown 解析

### 决策内容

项目详情页 `/projects/[id]` 新增“导入 Markdown”能力，在浏览器端读取本站导出的项目执行记录，恢复当前阶段、已完成内容、验证证据、风险、下一步行动、补充说明，并根据“已完成检查项”标题恢复交付检查进度。

### 决策原因

项目执行记录已经支持导出 Markdown，但导出后的文件无法回流到站内继续编辑。浏览器本地解析可以补齐项目实战的“填写、导出、归档、重新导入、继续执行”闭环，同时保持 GitHub + Vercel 静态部署，不引入账号、数据库和上传风险。

### 被否决方案

- 服务端上传解析：会增加接口、存储、权限和隐私处理复杂度。
- 自动导入任意 Markdown：容易误覆盖当前执行记录，必须限定本站执行记录格式。
- 只导入文本字段不恢复检查项：无法恢复项目真实交付进度，闭环不完整。

### 后续影响

- 导入优先兼容本站导出的项目执行记录 Markdown。
- 检查项恢复基于标题匹配；用户大幅改写检查项标题后可能无法完整恢复。
- 导入文件只在当前浏览器处理，不上传服务器。

## 2026-06-13 重新开启合盖继续运行 Codex 的系统级电源策略

### 决策内容

按用户最新要求，重新设置 MacBook 合盖后不影响 Codex 继续运行任务：

```bash
pmset -a sleep 0 displaysleep 0 disksleep 0 disablesleep 1
```

执行后底层配置为：

- Battery Power：`sleep 0`、`displaysleep 0`、`disksleep 0`
- AC Power：`sleep 0`、`displaysleep 0`、`disksleep 0`
- `SleepDisabled`：`true`

### 决策原因

用户再次明确要求设置 MacBook 关闭盖子也不影响 Codex 继续跑任务。Codex 进程自身持有空闲睡眠断言，但合盖场景需要系统级电源策略配合，因此重新开启 `disablesleep` 并关闭系统、显示器和磁盘睡眠计时。

### 被否决方案

- 只依赖 Codex 的 `NoIdleSleepAssertion`：不能充分覆盖合盖睡眠场景。
- 只运行临时 `caffeinate`：更适合短任务，无法持久表达当前用户要求。
- 忽略 2026-06-12 的取消记录：会破坏状态可追溯性；本次保留历史并追加最新决策。

### 后续影响

- 合盖运行会增加发热和耗电风险，建议连接电源并保持散热。
- 如需再次取消，可按 2026-06-12 记录恢复为 `SleepDisabled=false` 和改动前电源计时。

## 2026-06-13 复盘 Markdown 导入后采用手动另存历史

### 决策内容

复盘中心在成功导入 Markdown 后，不自动写入历史列表，而是在成功提示中提供“另存为历史”按钮。点击后把本次导入解析出的模板、输入字段、复盘报告和 Codex Prompt 写入 `codex-mastery:review-history`。

### 决策原因

导入文件可能只是为了继续编辑草稿，自动保存到历史会让历史列表出现用户并未确认的记录。手动另存既补齐“导入、恢复、沉淀”的闭环，又保留用户对本地历史资产的控制权。

### 被否决方案

- 导入成功后自动保存历史：容易产生重复记录，也可能保存用户不想保留的临时文件。
- 只保留导入提示文字：用户仍需要再找“保存历史”按钮，闭环不够清晰。
- 引入云端保存：当前站点保持静态部署和本地隐私策略，不增加账号与后端复杂度。

### 后续影响

- 导入历史仍只保存在当前浏览器 localStorage，清理浏览器数据会丢失。
- 如果用户导入后继续编辑，提示中的“另存为历史”保存的是导入时的快照；继续编辑后的内容仍可用原有“保存历史”按钮保存。

## 2026-06-12 提效小工具教程采用参考手册形态并加入风险边界

### 决策内容

本轮 Word 文档采用 `compact_reference_guide` 参考手册形态生成，定位为“内部学习与个人提效参考”。文档保留用户提供的工具入口和价格信息，但对“魔法梯子”“第三方 GPT Plus / 共享会员”等服务明确加入合规、隐私、稳定性、封号、售后和服务条款风险提示。

由于 Documents 技能自带 LibreOffice 渲染器缺少 `liblcms2.2.dylib`，本轮采用 Microsoft Word 导出 PDF，再用 Poppler 渲染 PNG 的方式完成视觉 QA。

### 决策原因

- 用户要求生成详细使用教程 Word 文档，教程型内容适合清晰目录、步骤、清单、表格和 Prompt 模板。
- 用户提供的部分工具涉及第三方账号、共享会员和网络连接工具，直接写成无风险推荐不合适，必须保留使用边界和安全提醒。
- 直接交付未渲染 DOCX 风险较高；在 LibreOffice 渲染失败时，Microsoft Word 原生导出 PDF 更接近用户实际打开效果。

### 被否决方案

- 写成纯推广文案：会弱化合规和隐私风险，不适合作为可长期使用的教程。
- 只给聊天文本不生成 DOCX：不满足用户明确的 Word 文档交付要求。
- 因 `render_docx.py` 失败而跳过视觉检查：会增加版面截断、孤立尾页和表格溢出的风险。
- 继续等待 Homebrew 补装依赖：本机网络下载卡住，继续等待会拖延交付。

### 后续影响

- 后续类似第三方工具教程应继续保留风险提示，不把共享账号或网络连接工具写成无条件推荐。
- 若后续修复本机 LibreOffice 依赖，可重新使用 `render_docx.py` 作为标准渲染路径。
- 本轮文档可通过脚本重新生成，避免手工编辑 DOCX 后难以追溯。

## 2026-06-12 取消合盖继续运行 Codex 的系统级电源策略

### 决策内容

按用户最新要求，撤销 2026-06-08 为“MacBook 合盖后不影响 Codex 继续运行任务”而做的系统级电源策略调整，恢复为当时改动前记录的配置：

```bash
pmset -b sleep 1 displaysleep 20 disksleep 10 disablesleep 0
pmset -c sleep 0 displaysleep 0 disksleep 10 disablesleep 0
```

恢复后底层配置为：

- Battery Power：`sleep 1`、`displaysleep 20`、`disksleep 10`
- AC Power：`sleep 0`、`displaysleep 0`、`disksleep 10`
- `SleepDisabled`：`false`

### 决策原因

用户明确要求“恢复原样，取消这个任务计划”。继续保留合盖不休眠配置会带来额外发热、耗电和安全风险，不符合用户当前意图。

### 被否决方案

- 继续保留 `disablesleep 1`：与用户最新要求冲突。
- 执行 `pmset restoredefaults`：可能改动超出 2026-06-08 本轮设置的其他电源项，不如按已有记录精确恢复。
- 删除历史记录：会损失可追溯性；本次采用追加撤销记录并更新当前状态。

### 后续影响

- 合盖后 macOS 可能进入睡眠，Codex 长任务可能暂停或中断。
- 如未来确需长任务不中断，应优先选择明确的临时方案，并在任务结束后及时恢复。

## 2026-06-10 最终稿采用单次 Artifact Tool 会话生成

### 决策内容

最终稿直接在同一个 Artifact Tool 会话中导入原始 `零信任模版.pptx`，复制 18 个映射页面、编辑继承元素并删除原始页面后导出，不重新导入裁剪后的 starter deck。

### 决策原因

- 裁剪后的 starter deck 保留了模板原有的重复批注锚点，重新导入时会触发重复元素键错误。
- 单次会话仍完整继承源模板页面、字体、配色、图形和布局，没有从空白页重建。
- 该方式可稳定生成 18 页 PPTX 和逐页 PNG、布局 JSON。

### 被否决方案

- 使用 Python 或直接修改 OOXML 绕过导入错误：会降低可追溯性并增加文件损坏风险。
- 从空白页重绘：不满足模板跟随要求。
- 混入准入模板原页：EMF 兼容性和视觉割裂风险较高。

### 后续影响

- 自动模板保真检查器会提示缺少 starter deck 导入证据，需结合源模板导入脚本、页映射和视觉渲染进行核验。
- 最终文件已通过 XML 校验、18 页全稿渲染和 Microsoft PowerPoint 原生打开验证。

## 2026-06-10 星宇车灯汇报采用零信任主模板与准入表达融合

### 决策内容

本次 18 页客户汇报以 `零信任模版.pptx` 作为最终 PPTX 的主视觉和可编辑页面骨架，通过 Artifact Tool 复制并编辑映射页面；`准入模版.pptx` 作为准入章节的内容和表达参考，重点吸收“可视、可控、安全、联动”以及终端全旅程的结构，不直接作为最终生产模板。

### 决策原因

- 用户明确要求整体视觉参考零信任模板，蓝白科技风需贯穿全稿。
- 零信任模板可被 Artifact Tool 完整导入、渲染和导出，适合保持可编辑结构。
- 准入模板含 EMF 图片，Artifact Tool 在第 10 页附近渲染失败，直接混用会增加兼容性和交付风险。
- 统一主模板可以避免零信任与准入章节出现两套视觉体系。

### 被否决方案

- 直接拼接两套模板页面：视觉割裂，且准入模板的 EMF 兼容问题会影响最终导出。
- 从空白页面重新绘制全部内容：无法满足模板跟随模式的继承要求。
- 沿用模板中的 XDLP、全球组网、商业秘密保护内容：与客户本次建设范围无关。

### 后续影响

- 每个输出页必须映射到零信任模板的源页并保留其字体、配色和页面结构。
- 准入页面需要在零信任主模板中重写文案和图示标签，不能直接复制准入模板的旧内容。
- 最终质检需同时检查模板保真、空占位符、中文字体和客户结论是否可见。

## 2026-06-09 项目实战详情页采用静态路由与本地执行记录

### 决策内容

为项目实战中心新增 `/projects/[id]` 动态详情页，使用 `generateStaticParams` 为 8 个项目生成静态详情路由。详情页继续从 `data/projects.json` 读取项目内容，并新增浏览器本地执行记录，保存在 `codex-mastery:project-execution-records`。项目检查清单继续共用 `codex-mastery:project-checklist`，列表页和详情页进度保持一致。

### 决策原因

项目卡片可以帮助用户快速扫描项目，但真实执行需要更完整的目标、步骤、Prompt、检查清单和阶段记录。详情页让每个项目成为独立交付页面，执行记录导出 Markdown 让用户能把项目进展沉淀到本地知识库、项目文档或复盘中心。

### 被否决方案

- 在项目卡片中继续堆叠所有内容：会让列表页信息密度过高，移动端扫描成本变大。
- 引入数据库保存执行记录：当前项目保持 GitHub + Vercel 静态部署，后端会增加账号、权限和隐私复杂度。
- 只做详情页不做执行记录：能阅读但不能沉淀交付过程，无法形成实战闭环。

### 后续影响

- 新增项目时必须保证 `id` 可作为详情页路径。
- 执行记录只保存在当前浏览器，清理浏览器数据会丢失。
- 如果未来需要跨设备同步，需要单独设计账号、权限和存储。

## 2026-06-09 复盘中心导入采用浏览器本地 Markdown 解析

### 决策内容

复盘中心新增“导入 Markdown”能力，在浏览器端读取本地 `.md` 或纯文本文件，识别复盘标题和 `## 输入摘要` 中的字段内容，再恢复到对应复盘模板草稿。导入成功后写入 `codex-mastery:review-drafts`，但不会自动写入历史列表。

### 决策原因

复盘中心已经支持导出 Markdown 和保存历史，但导出后的文件无法回到站内继续编辑，用户的复盘资产会断在本地文件。本地解析 Markdown 可以补齐“导出、保存、回流编辑”的闭环，同时保持 GitHub + Vercel 静态部署，不引入后端、账号和隐私风险。

### 被否决方案

- 服务端上传解析：会增加接口、存储和隐私处理复杂度，不符合当前静态部署策略。
- 只支持历史记录恢复：历史只在当前浏览器，无法恢复下载到本地的 Markdown 文件。
- 强依赖完全固定格式：用户可能手工编辑 Markdown，因此导入逻辑需要按标题和字段匹配度容错。

### 后续影响

- 导入优先兼容本站导出的复盘报告和 Codex Prompt 字段块。
- 如果后续调整复盘导出格式，需要同步更新导入解析逻辑。
- 导入不会上传文件，文件内容只在当前浏览器处理并写入本地草稿。

## 2026-06-09 项目实战中心采用可勾选交付检查清单

### 决策内容

为 8 个项目实战条目新增 `deliveryChecklist` 结构化字段，每个项目包含 5 条交付检查项。检查项由标题、验收标准和证据要求组成，并在 `/projects` 页面以可勾选清单展示。勾选状态保存在浏览器 localStorage 的 `codex-mastery:project-checklist`。

### 决策原因

项目实战中心原本包含目标、业务价值、实现步骤、Prompt、优化建议和成果物，但用户真正执行项目时仍缺少“做到什么程度才算完成”的判断依据。交付检查清单能把项目从展示内容推进为实战执行工具，并与 Learn Codex By Building 的定位一致。

### 被否决方案

- 只在项目描述里追加长文本：可读性差，也无法形成完成进度。
- 引入后端保存项目进度：当前站点保持 GitHub + Vercel 静态部署，后端会增加部署和权限复杂度。
- 只提供统一通用检查清单：不同项目的交付证据差异较大，通用清单无法指导真实执行。

### 后续影响

- 新增项目时必须补充 `deliveryChecklist`。
- 项目完成状态只保存在当前浏览器，清理浏览器数据后会丢失。
- `content/projects.md` 需要通过 `npm run export:content` 与 `data/projects.json` 保持同步。

## 2026-06-09 复盘历史采用浏览器本地列表

### 决策内容

复盘中心新增历史复盘列表，使用 `codex-mastery:review-history` 保存在浏览器 localStorage 中，最多保留最近 20 条记录。每条历史包含模板、保存时间、输入快照、复盘报告和给 Codex 的深度复盘 Prompt。

### 决策原因

复盘中心已经支持草稿、复制和导出 Markdown，但用户完成多次训练或项目复盘后，需要在站内快速找回最近记录并继续编辑。使用本地浏览器列表可以补齐个人学习闭环，同时不引入账号、数据库和云端隐私风险。

### 被否决方案

- 接入后端数据库：会增加部署和权限复杂度，超出当前静态站点阶段。
- 只依赖下载文件：可长期保存，但用户回到站内无法继续编辑。
- 无限保存历史：localStorage 空间有限，且历史太多会降低扫描效率。

### 后续影响

- 历史复盘只保存在当前浏览器，清理浏览器数据后会丢失。
- 未来如需跨设备同步，需要单独设计账号、权限和存储。
- 历史记录上限为 20 条，后续可按用户反馈调整。

## 2026-06-09 移动端全部模块抽屉采用分组列表

### 决策内容

移动端“全部模块”抽屉从 2 列模块格子升级为分组列表，按“学习路径、实战产出、业务工具”组织模块，并在顶部显示当前浏览模块。抽屉打开时锁定背景滚动，支持点击遮罩、关闭按钮和 Esc 关闭。

### 决策原因

当前站点模块数量已经达到 13 个，单纯图标格子虽然节省空间，但用户需要靠记忆理解模块用途。分组列表能用更少的认知成本说明“下一步该点哪里”，也更适合中文长标签和移动端单手浏览。

### 被否决方案

- 继续保留 2 列格子：扫描速度快，但模块意义不够清楚。
- 把所有模块放到底部 Tab：会挤压内容区，也不适合 13 个入口。
- 增加复杂动画：不符合当前克制、专业、Apple / Linear 风格。

### 后续影响

- 新增模块时需要补充 `group` 和 `description`。
- 移动端抽屉验证需要覆盖 375×812 视口、无横向溢出、关闭交互和当前模块标识。

## 2026-06-09 复盘报告导出采用浏览器本地 Markdown 文件

### 决策内容

复盘中心新增“导出 Markdown”按钮，在浏览器端基于当前复盘报告文本生成 `.md` 文件并下载，不引入后端、数据库或云端存储。

### 决策原因

复盘中心的核心价值是让用户把训练、Prompt 迭代、项目交付和售前工作沉淀为可保存资产。浏览器本地导出能满足个人学习与交付归档需求，同时保持 GitHub + Vercel 静态部署模型简单可靠。

### 被否决方案

- 引入服务端文件生成：会增加部署复杂度，与当前静态内容型站点定位不匹配。
- 自动同步到云端：需要账号、权限和隐私设计，超出当前任务范围。
- 只保留复制按钮：用户仍需要手动创建文件，复盘资产沉淀不够顺手。

### 后续影响

- 导出的文件只保存在用户本机下载目录。
- 后续如要做跨设备复盘档案，需要单独设计账号、权限和存储。
- 复盘模板内容仍以 `data/reviews.json` 为源，导出逻辑只负责当前报告文本。

## 2026-06-09 移动端导航采用常用入口加全部模块抽屉

### 决策内容

移动端不再展示完整横向滚动模块导航，而是展示 5 个常用入口：控制台、任务、30天、进度、复盘；其余模块通过顶部“模块”按钮打开“全部模块”抽屉访问。桌面端侧边栏保持不变。

### 决策原因

当前站点模块已经较多，移动端完整横向导航会变长，用户需要不断横向滑动才能找到模块。常用入口加全部模块抽屉能保持首屏稳定、减少误触，并让新增模块不继续挤压移动端导航。

### 被否决方案

- 保留完整横向滚动导航：模块增加后扫描成本高。
- 做底部固定 Tab Bar：会占用内容区高度，且当前顶部 header 已承担页面定位。
- 桌面和移动共用同一导航形态：无法兼顾桌面信息密度和手机可触达性。

### 后续影响

- 新增模块时需要判断是否进入 5 个常用入口；非高频模块默认进入全部模块抽屉。
- 移动端导航验证需要覆盖 375×812 视口和全部模块抽屉。

## 2026-06-09 复盘中心采用数据驱动模板与本地草稿

### 决策内容

新增 `/reviews` 复盘中心，复盘模板存储在 `data/reviews.json`，页面渲染通过 `ReviewKit` 类型约束；用户填写的复盘草稿存储在浏览器 localStorage 的 `codex-mastery:review-drafts`，不引入数据库和账号系统。

### 决策原因

复盘中心的目标是补齐学习闭环：用户完成训练、Prompt 迭代、项目交付、Bug 修复或安全售前拜访后，可以快速生成复盘报告和下一步 Codex Prompt。当前项目仍保持 GitHub + Vercel 轻量部署，本地草稿足以支撑个人学习场景。

### 被否决方案

- 引入后端保存复盘记录：会增加账号、数据库和隐私处理复杂度。
- 把复盘模板写死在组件里：不符合内容集中存储在 `data/*.json` 的项目规则。
- 只提供空白文本框：无法体现实战专家视角，也不能指导用户完成高质量复盘。

### 后续影响

- 新增复盘场景优先修改 `data/reviews.json`，再运行 `npm run export:content`。
- 换浏览器或清理浏览器数据会丢失未复制的本地草稿。
- 如后续需要跨设备复盘档案，需要单独设计账号与存储方案。

## 2026-06-09 视频中心只保留 Bilibili 且使用 B 站接口校验

### 决策内容

按用户最新反馈，视频中心从当前版本开始只保留 Bilibili 视频链接，并将 `npm run audit:videos` 从通用 HTTP 检查升级为 Bilibili 视频信息接口检查：

- `data/videos.json` 只允许 `platform = Bilibili`。
- 视频 URL 必须包含 BV 号。
- 巡检脚本调用 `https://api.bilibili.com/x/web-interface/view?bvid=...`。
- 只有接口返回 `code = 0` 的视频才允许保留。
- 删除 OpenAI Academy 视频和所有此前在浏览器中显示“视频不见了”的 BV 链接。

### 决策原因

Bilibili 的普通视频错误页可能仍返回 HTTP 200，旧巡检脚本会把“页面可访问”误判为“视频可播放”。用户已经在浏览器中确认多个旧 BV 链接打开后显示“视频不见了”，因此必须用更接近视频真实状态的 Bilibili 视频信息接口判断。

### 被否决方案

- 继续保留 OpenAI Academy：用户明确要求只保留哔哩哔哩视频链接。
- 继续使用 HTTP 200/3xx 作为视频可用标准：无法识别 B 站视频下架页。
- 保留失效视频并加提示：用户目标是打开即可学习，不接受无法打开的视频。

### 后续影响

- 新增或替换视频必须先运行 `npm run audit:videos`。
- 如果 Bilibili 接口返回非 `code=0`，对应视频必须删除或替换。
- 页面、类型和 README 均按“仅保留 Bilibili”维护。

## 2026-06-09 学习进度中心继续使用本地浏览器状态

### 决策内容

新增 `/progress` 学习进度中心，继续复用首页已有的 `codex-mastery:completed-days` localStorage 状态，不引入账号系统、数据库或云同步。

### 决策原因

当前项目定位是可直接部署到 GitHub + Vercel 的静态内容型学习平台，不依赖后端和私有环境变量。本地浏览器状态可以满足个人学习进度管理，同时保持部署简单和维护成本低。

### 被否决方案

- 引入登录和数据库：会显著增加部署复杂度，偏离当前轻量学习平台目标。
- 用独立的新 localStorage key：会导致首页进度和进度中心不一致。
- 只在 30 天页面展示进度：缺少集中管理、周进度和可复制进度报告。

### 后续影响

- 同一浏览器内首页和 `/progress` 共享进度。
- 换浏览器或清理浏览器数据后，本地进度会丢失。
- 如后续要跨设备同步，需要单独设计账号和存储方案。

## 2026-06-09 视频中心只保留当前确认可访问资源

### 决策内容

按用户最新要求，视频中心不再展示当前无法打开、无法正常播放或无法在本机验证可访问的视频。当前执行结果：

- 删除 3 条 `network_or_timeout` 的 YouTube 视频。
- 删除旧的 404 抖音精选 URL 引用。
- 保留 9 条当前命令行 HTTP 检查为 200 的视频资源。
- 当前视频平台仅保留 Bilibili 与 OpenAI Academy。

### 决策原因

学习平台的核心体验是用户点击即可学习。对中国用户而言，无法打开、无法播放或需要特殊网络条件的视频会造成挫败感，因此比“平台覆盖完整”更重要的是“当前可访问、可使用”。

### 被否决方案

- 继续保留 YouTube 并标注“网络不可判定”：用户明确要求不能正常播放的全部去除。
- 保留抖音精选 404 链接供人工复核：会让用户继续点到失效内容。
- 用不可验证链接补足数量：会牺牲可用性。

### 后续影响

- 后续新增视频必须先验证可访问，再写入 `data/videos.json`。
- 如需恢复 YouTube、抖音精选等平台，必须先确认对应 URL 在目标用户环境中能正常打开播放。
- `content/videos.md` 必须通过 `npm run export:content` 与数据源保持一致。

## 2026-06-09 外部视频链接采用健康状态标记而非直接删除

### 决策内容

为 `VideoItem` 增加可选 `linkStatus` 字段，并在视频卡片中显示链接健康状态徽标：

- `ok`：链接可访问。
- `network_or_timeout`：当前网络无法可靠判定。
- `manual_review`：需要人工复核。
- `likely_broken`：疑似失效。

当前巡检中，Bilibili 链接标记为可访问；YouTube 链接因本机命令行网络超时标记为网络不可判定；抖音精选链接因 HTTP 404 标记为疑似失效；OpenAI Academy 旧链接已替换为当前可访问的官方链接。

### 决策原因

视频内容是学习平台的一部分，外部平台链接会因地区网络、平台反爬、登录态、内容下架或 URL 迁移而变化。直接删除所有异常链接会造成内容突然缺失；保留并标注状态，可以让用户理解风险，也方便后续替换。

### 被否决方案

- 直接删除 YouTube 和抖音链接：YouTube 当前只是命令行网络超时，不能判定内容失效；抖音链接也需要先保留证据以便替换。
- 不显示链接状态：用户会点击到 404 或超时页面，体验更差。
- 只在内部文档记录：用户无法在页面上识别风险，不符合交互体验优化目标。

### 后续影响

- 后续新增视频时应填写或巡检 `linkStatus`。
- 定期巡检外部视频链接时，只需要更新 `data/videos.json` 并运行 `npm run export:content`。
- 当前标记为 `likely_broken` 的抖音精选链接应优先寻找可信替代来源。

## 2026-06-08 初始化项目记忆机制

### 决策内容

在项目根目录创建并维护 `PROJECT_CONTEXT.md`、`TASK_STATE.md`、`DECISIONS.md`、`CHANGELOG_AI.md`。

### 决策原因

用户提供的项目规则要求优先保证项目状态不丢失，并支持会话压缩、切换账号、切换模型或系统重启后的持续接管。

### 被否决方案

- 仅依赖聊天上下文：不符合项目规则，且无法跨会话可靠恢复。
- 仅依赖 Git 历史：当前目录不是 Git 仓库，无法使用。

### 后续影响

- 后续每次阶段性进展都需要更新 `TASK_STATE.md`。
- 重要架构、数据、API、权限、部署和技术选型决策需要记录到本文件。
- 修改代码或内容时需要同步更新 `CHANGELOG_AI.md`。

## 2026-06-08 切换为项目执行负责人工作流

### 决策内容

从本轮开始，按用户确认的项目执行负责人流程推进项目：

- 先理解需求、识别缺失信息和风险。
- 大规模开发前输出计划并等待确认。
- 一次只执行一个明确任务。
- 每个任务结束后更新 `PROJECT_STATE.md`。
- 重要产品、技术和流程决策继续记录到 `DECISIONS.md`。
- 任务变长或上下文风险升高时创建 `HANDOVER.md`。

### 决策原因

当前项目已经具备可运行基础，但后续还涉及内容质量、交互体验、部署和持续维护。若继续以快速改代码方式推进，容易出现状态丢失、目标漂移和变更不可追溯的问题。

### 被否决方案

- 直接继续重构页面：未经过用户确认，且会扩大风险。
- 删除旧状态文件：可能丢失历史上下文。
- 同时推进多个大型任务：不符合用户要求的一次一个任务原则。

### 后续影响

- `PROJECT_STATE.md` 成为当前项目状态的主记录。
- `TASK_STATE.md` 作为历史兼容文件保留，但不再作为唯一状态源。
- 后续每次任务结束必须更新 `PROJECT_STATE.md`。
- 大范围功能、架构、部署或内容策略调整前必须先确认。

## 2026-06-08 明确本地构建与开发服务隔离规则

### 决策内容

本地执行生产构建检查时，优先停止 `npm run dev`，再运行 `npm run build`。如果构建后 `localhost:3000` 出现 500，按以下方式恢复：

```bash
rm -rf .next
npm run dev
```

### 决策原因

健康检查中发现：`next dev` 与 `next build` 同时读写 `.next` 时，开发服务可能出现 React Server Components manifest 不一致，导致首页短暂 500。干净构建和生产服务验证均通过，说明不是业务代码错误，而是本地运行流程问题。

### 被否决方案

- 忽略该问题：会导致用户再次遇到“网址打不开”。
- 修改业务代码规避：根因不在页面代码，修改业务代码会引入无关风险。
- 删除生产构建检查：会降低 Vercel 部署前的质量把关。

### 后续影响

- README 和 DEPLOY 已记录恢复方法。
- 后续健康检查需要区分 dev server 验证和生产构建验证。
- 结束任务前必须保证 `localhost:3000` 恢复可访问。

## 2026-06-08 首页学习进度增加撤销入口

### 决策内容

首页学习进度保留“完成 Day X”的主操作，并新增“撤销最近完成”次级操作。Prompt 统计从固定数字改为基于 `prompts` 数据动态计算。

### 决策原因

移动端与交互体验检查发现：首页完成 Day 1 后会直接推进到 Day 2，用户缺少可见的撤销入口。动态统计可以避免 `data/prompts.json` 已有 22 条 AI Prompt 但首页仍显示 20 的数据不一致。

### 被否决方案

- 保持原样：用户误点后无法从首页直接恢复。
- 做复杂日历式进度管理：超出本轮检查范围，会扩大改动。
- 继续写死统计数字：后续内容扩展时容易再次不一致。

### 后续影响

- 首页进度交互更可恢复。
- 首页统计会随数据变化自动更新。
- 后续如需要完整进度管理，可在确认后单独设计“进度中心”。

## 2026-06-08 本地 Git 初始化并暂缓远程创建

### 决策内容

在当前项目目录初始化本地 Git 仓库，使用 `main` 作为默认分支，并配置仓库本地提交身份：

```text
Codex Mastery <codex-mastery@local>
```

远程 GitHub 仓库创建和 push 暂缓，等待 GitHub CLI 安装/登录，或等待用户提供 GitHub 空仓库地址。

### 决策原因

用户已授权执行本地 Git 初始化和首次提交，但本机未安装 `gh` GitHub CLI，无法直接检查登录状态、创建 GitHub 仓库或推送。创建 GitHub 账号涉及第三方注册、邮箱/手机号/验证码和服务条款确认，不能由我代替用户完成。

### 被否决方案

- 继续尝试创建 GitHub 账号：涉及用户身份和第三方账号注册，不适合由自动化代办。
- 修改全局 Git 用户配置：会影响用户机器上的其他项目。
- 在没有远程仓库地址的情况下猜测 push 目标：不可执行且风险高。

### 后续影响

- 当前项目从本地 Git 历史开始受保护。
- 如果后续安装并登录 `gh`，可以继续自动创建仓库并推送。
- 如果用户提供 GitHub 空仓库 URL，可以直接配置 `origin` 并推送。

## 2026-06-08 调整本机电源策略以支持 Codex 长任务

### 决策内容

按用户要求，在本机 macOS 上通过 `pmset` 调整电源策略，降低 Codex 长任务因合盖或空闲睡眠中断的风险：

```bash
pmset -a sleep 0 displaysleep 0 disksleep 0 disablesleep 1
```

### 决策原因

用户希望 MacBook 合盖后不影响 Codex 继续运行任务。Codex 当前虽持有空闲睡眠断言，但空闲睡眠断言通常不能完全覆盖合盖触发的系统睡眠，因此需要系统级电源策略配合。

### 被否决方案

- 仅依赖 Codex 进程的 `NoIdleSleepAssertion`：只能降低空闲睡眠风险，不能充分保证合盖场景。
- 仅运行 `caffeinate`：适合临时防止空闲睡眠，但对合盖场景不如系统级策略明确。
- 安装第三方常驻工具：当前需求可先通过 macOS 原生命令满足，无需引入额外依赖。

### 后续影响

- 合盖长时间运行会增加发热和耗电风险，建议连接电源并保持散热。
- 如需恢复默认电源策略，可运行 `sudo pmset restoredefaults`。
- 后续长任务前可用 `pmset -g custom` 和 `pmset -g assertions` 检查当前状态。

## 2026-06-09 Vercel 直接部署策略

### 决策内容

在 GitHub push 仍受认证阻塞时，优先尝试通过 Vercel CLI 直接从本地项目部署到已有 Vercel 项目 `crh-s-projects/codex-mastery`。CLI 通过 `npx --yes vercel@latest` 启动，不要求全局安装。

### 决策原因

用户要求接管 Vercel 项目并完成公网部署。当前本地项目已经通过 `npm run typecheck`、`npm run lint` 和 `npm run build`，代码本身具备部署条件；GitHub 自动推送暂时卡在认证层，因此 Vercel CLI 直传是最短可行路径。

### 被否决方案

- 在聊天中索要 Vercel 密码、验证码或长期 token：风险过高，不符合账号安全原则。
- 绕过 Vercel 官方授权流程：不可接受。
- 在未完成本地构建验证前直接部署：会把可在本地发现的问题推到公网环境。

### 后续影响

- 部署前继续以本地质量门禁为准：`npm run typecheck`、`npm run lint`、`npm run build`。
- 如果 Vercel CLI 未登录，需要用户在 Vercel 官方授权页面完成确认，或由用户本人创建临时 `VERCEL_TOKEN`。
- 部署成功后需要验证公网 URL 返回 200，并更新 `PROJECT_STATE.md`、`TASK_STATE.md`、`CHANGELOG_AI.md`。

## 2026-06-09 使用 vercel.json 固定 Next.js 部署配置

### 决策内容

在项目根目录新增 `vercel.json`，显式声明：

- `framework`: `nextjs`
- `buildCommand`: `npm run build`
- `installCommand`: `npm install`
- `outputDirectory`: `null`

并通过 `vercel deploy --prod --yes --force --scope crh-s-projects` 重新部署到 `crh-s-projects/ronghuacao66-lang-codex-mastery`。

### 决策原因

Vercel 项目远端设置显示 `Framework Preset: Other`，输出目录为“存在 `public` 则使用 `public`，否则使用 `.`”。这会让 Next.js App Router 项目有被当成普通静态项目处理的风险，表现为主域名显示 `404: NOT_FOUND`。仓库内 `vercel.json` 可以把部署配置固化到代码中，避免 GitHub 导入或后续部署时丢失 Next.js 框架识别。

### 被否决方案

- 只依赖 Vercel Dashboard 手动设置：不可追溯，且后续换账号或重新导入时容易丢失。
- 只重新部署不改配置：可能再次生成错误产物或错误路由。
- 将项目改为静态导出：当前应用使用 Next.js App Router，无需为了部署绕开框架能力。

### 后续影响

- `vercel.json` 必须提交到 GitHub。
- 后续 Vercel 自动部署应读取该文件并按 Next.js 构建。
- Vercel Dashboard 仍可能显示项目级 `Framework Preset: Other`，但仓库配置优先保障部署产物正确。

## 2026-06-09 合并 GitHub 网页上传历史但保留本地正确结构

### 决策内容

远程 GitHub 仓库 `origin/main` 可读取，但其历史来自网页上传，当前文件位于旧的 `codex-mastery-github-ready/` 嵌套目录，并包含旧 zip。为了后续可以普通 push，本地执行：

```bash
git merge --allow-unrelated-histories -s ours origin/main -m "Merge remote upload history"
```

该合并记录远程历史为第二父提交，但保留本地正确项目根目录结构。

### 决策原因

本地历史和远程网页上传历史无共同祖先。若不处理，后续即使 GitHub 凭据可用，普通 push 也会因非 fast-forward 被拒绝；若直接强推，会覆盖远程历史。`ours` 合并可以保留远程历史，同时让本地 `main` 成为远程 `main` 的后代。

### 被否决方案

- 强推覆盖远程：风险更高，不符合默认安全策略。
- 合并远程文件树：会把旧的嵌套目录和 zip 带回项目，破坏根目录结构。
- 继续只提供手动上传包：无法形成可追溯 GitHub 提交历史。

### 后续影响

- 后续 GitHub 凭据可用后，`git push -u origin main` 应可作为 fast-forward push 执行。
- push 成功后，远程仓库根目录会变为当前正确项目结构，并包含 `vercel.json`。

## 2026-06-09 准备项目专用 GitHub SSH key

### 决策内容

在 GitHub CLI Web 授权两次因 OAuth token 交换超时失败后，改用 SSH key 作为 GitHub push 的备用认证路径。已生成项目专用 Ed25519 key：

```text
~/.ssh/codex_maturity_github_ed25519
~/.ssh/codex_maturity_github_ed25519.pub
```

并在 `~/.ssh/config` 中配置 `github.com` 使用该 key。

### 决策原因

当前 HTTPS push 缺少 GitHub credential，GitHub CLI 虽已安装但无法完成 OAuth token 交换。SSH key 方式不依赖 GitHub CLI OAuth token，只需要用户在 GitHub 账号安全设置中添加公钥。

### 被否决方案

- 在聊天中索要 GitHub token：不安全，不采用。
- 继续反复重试 OAuth：当前已经两次超时，继续重试收益低。
- 强推远程：与认证问题无关，且风险更高。

### 后续影响

- 用户需要在 GitHub 页面添加剪贴板中的公钥。
- 公钥添加后，应先运行 `ssh -T git@github.com` 验证，再切换 remote 为 SSH 并 push。
- 私钥保存在本机，不提交到项目仓库。
